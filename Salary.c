#include<stdio.h>
int main(){
   double NUMBER,h,m,SALARY;
   scanf("%lf %lf %lf",&NUMBER,&h,&m);
   NUMBER = NUMBER;
   SALARY = h*m;
   printf("NUMBER = %.0lf\nSALARY = U$ %.2lf\n",NUMBER,h*m);
return 0;
}
